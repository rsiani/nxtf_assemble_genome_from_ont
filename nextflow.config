conda {
    enabled = true
    useMicromamba = true
}

nextflow {
    workDir = './work'
    tmpDir  = './work/tmp'
}

process {
    executor     = 'local'
    memory       = '10 GB'
    cpus         = 5
    errorStrategy = 'finish'
    maxRetries   = 1
    conda        = "./environment.yml" 
}